#numbers = [1,2,3,4]

#for number in numbers:
   # print(number)

colors = ["blue", "teal", True, 8.9]

for color in colors:
    print(color)
index = 0
while index < len(colors):
    print(colors[index])
    index += 1

sounds = ["super", "cali", "fragil", "istic", "expi", "ali", "docious"]

# Define your code below:
index = 0
while index < len(sounds):
    result = print(sounds[index])
    index += 1

    sounds = ["super", "cali", "fragil", "istic", "expi", "ali", "docious"]

    # Define your code below:
    result = ''
    for s in sounds:
        result += s.upper()
